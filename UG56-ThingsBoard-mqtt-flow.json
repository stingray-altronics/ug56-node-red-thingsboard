[
  {
    "id": "openlab_registry_v2",
    "type": "tab",
    "label": "Open Lab UG56 MQTT - Registry",
    "disabled": false,
    "info": "v2: DEVICE_REGISTRY, helpers LT + intervalos y emisor generico. Leer README.md. Deshabilitar version anterior antes de Deploy. Sin credenciales exportadas. Node-RED 2.1+; no requiere paquetes adicionales. ENABLE_DOWNLINK=false simula.",
    "env": [
      {
        "name": "DEVICE_REGISTRY",
        "type": "json",
        "value": "{}"
      },
      {
        "name": "LNS_UPLINK_TOPIC",
        "type": "str",
        "value": "application/2/device/+/rx"
      },
      {
        "name": "ENABLE_DOWNLINK",
        "type": "bool",
        "value": "false"
      },
      {
        "name": "STATE_MAX_AGE_S",
        "type": "num",
        "value": "900"
      },
      {
        "name": "MIN_COMMAND_GAP_S",
        "type": "num",
        "value": "2"
      }
    ]
  },
  {
    "id": "registry_v2_guide",
    "type": "comment",
    "z": "openlab_registry_v2",
    "name": "1. Editar DEVICE_REGISTRY y credenciales; iniciar en simulacion",
    "x": 410,
    "y": 40,
    "wires": [],
    "info": "Una entrada por DevEUI: model LT22222 o AM102, applicationId, fPort. stateMaxAgeS opcional. No se usan LT_DEVEUI ni AM_DEVEUI. Uplink generico sigue aceptando equipos fuera del registro. Solo equipos registrados aceptan RPC."
  },
  {
    "id": "registry_v2_up",
    "type": "mqtt in",
    "z": "openlab_registry_v2",
    "name": "Uplinks LNS local",
    "x": 160,
    "y": 130,
    "wires": [
      [
        "registry_v2_normalize",
        "registry_v2_rawdebug"
      ]
    ],
    "topic": "${LNS_UPLINK_TOPIC}",
    "qos": "0",
    "datatype": "json",
    "broker": "registry_v2_lns",
    "nl": false,
    "rap": true,
    "rh": 0,
    "inputs": 0
  },
  {
    "id": "registry_v2_rawdebug",
    "type": "debug",
    "z": "openlab_registry_v2",
    "name": "MQTT LNS: mensaje completo",
    "x": 450,
    "y": 90,
    "wires": [],
    "active": false,
    "tosidebar": true,
    "console": false,
    "tostatus": false,
    "complete": "true",
    "targetType": "full"
  },
  {
    "id": "registry_v2_normalize",
    "type": "function",
    "z": "openlab_registry_v2",
    "name": "Uplink generico: object a ThingsBoard",
    "x": 440,
    "y": 150,
    "wires": [
      [
        "registry_v2_ltstate"
      ]
    ],
    "func": "try {\n    const hasEui = p => p && typeof p === 'object' && (p.deveui || p.devEUI || p.eui);\n    let p = hasEui(msg) ? msg : msg.payload;\n    if (Buffer.isBuffer(p)) p = p.toString('utf8');\n    if (typeof p === 'string') p = JSON.parse(p);\n    if (!p || typeof p !== 'object' || Array.isArray(p)) throw Error('Uplink JSON invalido');\n    const eui = String(p.deveui || p.devEUI || p.eui || '').trim().toLowerCase();\n    if (!/^[0-9a-f]{16}$/.test(eui)) throw Error('DevEUI invalido');\n    let decoded = p.object;\n    if (typeof decoded === 'string') decoded = JSON.parse(decoded);\n    if (!decoded || typeof decoded !== 'object' || Array.isArray(decoded)) {\n      throw Error('Falta object decodificado: asignar codec en LNS; no se interpreta el payload binario');\n    }\n    const values = {};\n    Object.keys(decoded).forEach(k => {\n      if (['__proto__','constructor','prototype'].includes(k)) return;\n      const v = decoded[k];\n      if (typeof v === 'string' || typeof v === 'boolean' || (typeof v === 'number' && Number.isFinite(v))) values[k] = v;\n      // Preserve compound values as JSON text without guessing flattening rules.\n      else if (v !== null && typeof v === 'object') values[k] = JSON.stringify(v);\n    });\n    if (!Object.keys(values).length) throw Error('object no contiene valores publicables');\n    const rx = Array.isArray(p.rxInfo) ? (p.rxInfo[0] || {}) : {};\n    const tx = p.txInfo || {};\n    const dr = p.dr || tx.dataRate || {};\n    const first = (...items) => items.find(x => x !== undefined && x !== null);\n    const metadata = {rssi:first(p.rssi,rx.rssi),snr:first(p.snr,rx.loRaSNR),\n      frame_count:first(p.fcnt,p.fCnt,p.frameCount),fport:first(p.fport,p.fPort,p.port),\n      frequency:first(p.freq,p.frequency,tx.frequency),\n      spreading_factor:dr.spreadFactor,bandwidth_khz:dr.bandwidth};\n    // Codec keys take precedence; radio metadata never overwrites sensor data.\n    Object.keys(metadata).forEach(k => {\n      if (!Object.prototype.hasOwnProperty.call(values,k) && typeof metadata[k] === 'number' && Number.isFinite(metadata[k])) values[k] = metadata[k];\n    });\n    const time = first(p.time,rx.time);\n    let ts = typeof time === 'string' ? Date.parse(time) : NaN;\n    const source = !Number.isFinite(ts) || ts > Date.now()+300000 ? 'node_red' : 'gateway';\n    if (source === 'node_red') ts = Date.now();\n    if (!Object.prototype.hasOwnProperty.call(values,'timestamp_source')) values.timestamp_source = source;\n    const seen = flow.get('uplink_devices') || [];\n    if (!seen.includes(eui)) {seen.push(eui);flow.set('uplink_devices',seen);}\n    const connect = {topic:'v1/gateway/connect',payload:{device:eui},qos:'1',retain:false};\n    const telemetry = {topic:'v1/gateway/telemetry',payload:{[eui]:[{ts,values}]},qos:'1',retain:false};\n    return [[connect,telemetry]];\n  } catch (err) {node.error(err.message,msg);return null;}",
    "outputs": 1,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": []
  },
  {
    "id": "registry_v2_ltstate",
    "type": "function",
    "z": "openlab_registry_v2",
    "name": "Estados LT por dispositivo y salida",
    "x": 760,
    "y": 150,
    "wires": [
      [
        "registry_v2_tbout",
        "registry_v2_telemetrydebug"
      ]
    ],
    "func": "const readRegistry = function readRegistry(env) {\n  let raw = env.get('DEVICE_REGISTRY');\n  if (typeof raw === 'string') raw = JSON.parse(raw);\n  if (!raw || typeof raw !== 'object' || Array.isArray(raw)) throw Error('DEVICE_REGISTRY debe ser un objeto JSON');\n  const devices = {};\n  for (const [key, entry] of Object.entries(raw)) {\n    const eui = key.toLowerCase();\n    if (!/^[0-9a-f]{16}$/.test(eui) || Object.prototype.hasOwnProperty.call(devices,eui)) throw Error('DevEUI invalido o duplicado en DEVICE_REGISTRY');\n    if (!entry || typeof entry !== 'object' || Array.isArray(entry)) throw Error('Entrada de registro invalida: '+eui);\n    const model = entry.model;\n    if (!['LT22222','AM102'].includes(model)) throw Error('Modelo no soportado en registro: '+eui);\n    const app = entry.applicationId;\n    if (!((typeof app === 'string' && /^\\d+$/.test(app)) || (Number.isSafeInteger(app) && app >= 0))) throw Error('applicationId invalido: '+eui);\n    if (!Number.isInteger(entry.fPort) || entry.fPort < 1 || entry.fPort > 223) throw Error('fPort debe ser entero 1..223: '+eui);\n    if (entry.stateMaxAgeS !== undefined && (!Number.isFinite(entry.stateMaxAgeS) || entry.stateMaxAgeS <= 0)) throw Error('stateMaxAgeS debe ser positivo: '+eui);\n    devices[eui] = {devEUI:eui,model,applicationId:String(app),fPort:entry.fPort,stateMaxAgeS:entry.stateMaxAgeS};\n  }\n  return devices;\n};\nconst reply = function reply(rpc, data) {\n  return {topic:'v1/gateway/rpc',payload:{device:rpc.device,id:rpc.id,data},qos:'1',retain:false};\n};\nif (msg.topic !== 'v1/gateway/telemetry') return msg;\n  try {\n    const registry = readRegistry(env);\n    const all = flow.get('registry_output_state') || {};\n    for (const [eui,samples] of Object.entries(msg.payload || {})) {\n      if (!registry[eui] || registry[eui].model !== 'LT22222' || !Array.isArray(samples)) continue;\n      const state = all[eui] || {};\n      for (const sample of samples) {\n        const v = sample.values;\n        if (!v || !Number.isFinite(sample.ts)) continue;\n        for (const output of ['RO1','RO2','DO1','DO2']) {\n          const raw = v[output+'_status'];\n          const valid = output.startsWith('RO') ? ['ON','OFF'] : ['L','H'];\n          if (!valid.includes(raw)) continue;\n          const active = raw === valid[0];\n          v[output.toLowerCase()+'_active'] = active;\n          if (!state[output] || sample.ts >= state[output].ts) state[output] = {value:active,ts:sample.ts};\n        }\n      }\n      all[eui] = state;\n    }\n    flow.set('registry_output_state',all);\n  } catch (err) {node.error(err.message,msg);}\n  // Registry errors never discard otherwise valid generic telemetry.\n  return msg;",
    "outputs": 1,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "outputLabels": []
  },
  {
    "id": "registry_v2_tbout",
    "type": "mqtt out",
    "z": "openlab_registry_v2",
    "name": "ThingsBoard Gateway API",
    "x": 1140,
    "y": 240,
    "wires": [],
    "topic": "",
    "qos": "1",
    "retain": "false",
    "broker": "registry_v2_tb"
  },
  {
    "id": "registry_v2_telemetrydebug",
    "type": "debug",
    "z": "openlab_registry_v2",
    "name": "Telemetria enviada",
    "x": 1060,
    "y": 110,
    "wires": [],
    "active": false,
    "tosidebar": true,
    "console": false,
    "tostatus": false,
    "complete": "true",
    "targetType": "full"
  },
  {
    "id": "registry_v2_manual",
    "type": "inject",
    "z": "openlab_registry_v2",
    "name": "Registrar dispositivos conocidos",
    "x": 170,
    "y": 240,
    "wires": [
      [
        "registry_v2_register"
      ]
    ],
    "props": [
      {
        "p": "payload"
      }
    ],
    "payload": "",
    "payloadType": "date",
    "repeat": "",
    "crontab": "",
    "once": true,
    "onceDelay": 3
  },
  {
    "id": "registry_v2_tbstatus",
    "type": "status",
    "z": "openlab_registry_v2",
    "name": "Reconexion ThingsBoard",
    "x": 160,
    "y": 300,
    "wires": [
      [
        "registry_v2_connected"
      ]
    ],
    "scope": [
      "registry_v2_tbout"
    ]
  },
  {
    "id": "registry_v2_connected",
    "type": "function",
    "z": "openlab_registry_v2",
    "name": "Solo al conectar",
    "x": 440,
    "y": 300,
    "wires": [
      [
        "registry_v2_register"
      ]
    ],
    "func": "if (msg.status && msg.status.fill === 'green' && msg.status.shape === 'dot') return msg;\nreturn null;",
    "outputs": 1,
    "noerr": 0
  },
  {
    "id": "registry_v2_register",
    "type": "function",
    "z": "openlab_registry_v2",
    "name": "Registrar registro y equipos descubiertos",
    "x": 760,
    "y": 240,
    "wires": [
      [
        "registry_v2_tbout"
      ]
    ],
    "func": "const readRegistry = function readRegistry(env) {\n  let raw = env.get('DEVICE_REGISTRY');\n  if (typeof raw === 'string') raw = JSON.parse(raw);\n  if (!raw || typeof raw !== 'object' || Array.isArray(raw)) throw Error('DEVICE_REGISTRY debe ser un objeto JSON');\n  const devices = {};\n  for (const [key, entry] of Object.entries(raw)) {\n    const eui = key.toLowerCase();\n    if (!/^[0-9a-f]{16}$/.test(eui) || Object.prototype.hasOwnProperty.call(devices,eui)) throw Error('DevEUI invalido o duplicado en DEVICE_REGISTRY');\n    if (!entry || typeof entry !== 'object' || Array.isArray(entry)) throw Error('Entrada de registro invalida: '+eui);\n    const model = entry.model;\n    if (!['LT22222','AM102'].includes(model)) throw Error('Modelo no soportado en registro: '+eui);\n    const app = entry.applicationId;\n    if (!((typeof app === 'string' && /^\\d+$/.test(app)) || (Number.isSafeInteger(app) && app >= 0))) throw Error('applicationId invalido: '+eui);\n    if (!Number.isInteger(entry.fPort) || entry.fPort < 1 || entry.fPort > 223) throw Error('fPort debe ser entero 1..223: '+eui);\n    if (entry.stateMaxAgeS !== undefined && (!Number.isFinite(entry.stateMaxAgeS) || entry.stateMaxAgeS <= 0)) throw Error('stateMaxAgeS debe ser positivo: '+eui);\n    devices[eui] = {devEUI:eui,model,applicationId:String(app),fPort:entry.fPort,stateMaxAgeS:entry.stateMaxAgeS};\n  }\n  return devices;\n};\nconst reply = function reply(rpc, data) {\n  return {topic:'v1/gateway/rpc',payload:{device:rpc.device,id:rpc.id,data},qos:'1',retain:false};\n};\nlet configured = [];\n  try {configured = Object.keys(readRegistry(env));} catch (err) {node.error(err.message,msg);}\n  const ids = [...new Set([...(flow.get('uplink_devices') || []),...configured])].filter(eui=>/^[0-9a-f]{16}$/.test(eui));\n  return ids.length ? [ids.map(device=>({topic:'v1/gateway/connect',payload:{device},qos:'1',retain:false}))] : null;",
    "outputs": 1,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "outputLabels": []
  },
  {
    "id": "registry_v2_rpcnote",
    "type": "comment",
    "z": "openlab_registry_v2",
    "name": "2. RPC -> registro -> helper -> emisor generico",
    "x": 360,
    "y": 380,
    "wires": [],
    "info": "Control LT: get/setRO1, RO2, DO1, DO2. Intervalo LT/AM102: setReportingInterval con params numerico en segundos. Se responde al preparar el mensaje, sin esperar confirmacion fisica. AM102 Clase A recibe al abrir sus ventanas tras un uplink."
  },
  {
    "id": "registry_v2_rpcin",
    "type": "mqtt in",
    "z": "openlab_registry_v2",
    "name": "RPC ThingsBoard",
    "x": 150,
    "y": 460,
    "wires": [
      [
        "registry_v2_route",
        "registry_v2_rpcdebug"
      ]
    ],
    "topic": "v1/gateway/rpc",
    "qos": "1",
    "datatype": "json",
    "broker": "registry_v2_tb",
    "nl": false,
    "rap": true,
    "rh": 0,
    "inputs": 0
  },
  {
    "id": "registry_v2_route",
    "type": "function",
    "z": "openlab_registry_v2",
    "name": "Resolver dispositivo y metodo",
    "x": 430,
    "y": 460,
    "wires": [
      [
        "registry_v2_ltcontrol"
      ],
      [
        "registry_v2_interval"
      ],
      [
        "registry_v2_tbout",
        "registry_v2_replydebug"
      ]
    ],
    "func": "const readRegistry = function readRegistry(env) {\n  let raw = env.get('DEVICE_REGISTRY');\n  if (typeof raw === 'string') raw = JSON.parse(raw);\n  if (!raw || typeof raw !== 'object' || Array.isArray(raw)) throw Error('DEVICE_REGISTRY debe ser un objeto JSON');\n  const devices = {};\n  for (const [key, entry] of Object.entries(raw)) {\n    const eui = key.toLowerCase();\n    if (!/^[0-9a-f]{16}$/.test(eui) || Object.prototype.hasOwnProperty.call(devices,eui)) throw Error('DevEUI invalido o duplicado en DEVICE_REGISTRY');\n    if (!entry || typeof entry !== 'object' || Array.isArray(entry)) throw Error('Entrada de registro invalida: '+eui);\n    const model = entry.model;\n    if (!['LT22222','AM102'].includes(model)) throw Error('Modelo no soportado en registro: '+eui);\n    const app = entry.applicationId;\n    if (!((typeof app === 'string' && /^\\d+$/.test(app)) || (Number.isSafeInteger(app) && app >= 0))) throw Error('applicationId invalido: '+eui);\n    if (!Number.isInteger(entry.fPort) || entry.fPort < 1 || entry.fPort > 223) throw Error('fPort debe ser entero 1..223: '+eui);\n    if (entry.stateMaxAgeS !== undefined && (!Number.isFinite(entry.stateMaxAgeS) || entry.stateMaxAgeS <= 0)) throw Error('stateMaxAgeS debe ser positivo: '+eui);\n    devices[eui] = {devEUI:eui,model,applicationId:String(app),fPort:entry.fPort,stateMaxAgeS:entry.stateMaxAgeS};\n  }\n  return devices;\n};\nconst reply = function reply(rpc, data) {\n  return {topic:'v1/gateway/rpc',payload:{device:rpc.device,id:rpc.id,data},qos:'1',retain:false};\n};\nlet p;\n  try {\n    p = msg.payload;\n    if (Buffer.isBuffer(p)) p = p.toString('utf8');\n    if (typeof p === 'string') p = JSON.parse(p);\n    // Replies use a top-level id, not data.id: never process reply echoes.\n    if (!p || !p.data || typeof p.data.method !== 'string') return null;\n  } catch (err) {node.error('RPC JSON invalido',msg);return null;}\n  if (typeof p.device !== 'string' || !Number.isSafeInteger(p.data.id) || p.data.id < 0) {\n    node.error('RPC sin device o id numerico valido',msg);return null;\n  }\n  const rpc = {device:p.device,eui:p.device.toLowerCase(),id:p.data.id,method:p.data.method,params:p.data.params};\n  try {\n    const device = readRegistry(env)[rpc.eui];\n    if (!device) throw Error('Dispositivo no registrado para downlink');\n    const next = {rpc,device};\n    if (/^(get|set)(RO1|RO2|DO1|DO2)$/.test(rpc.method)) {\n      if (device.model !== 'LT22222') throw Error('Control RO/DO solo disponible para LT22222');\n      return [next,null,null];\n    }\n    if (rpc.method === 'setReportingInterval') return [null,next,null];\n    throw Error('Metodo no soportado: '+rpc.method);\n  } catch (err) {return [null,null,reply(rpc,{accepted:false,error:err.message})];}",
    "outputs": 3,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "outputLabels": [
      "Control LT",
      "Intervalo",
      "Error RPC"
    ]
  },
  {
    "id": "registry_v2_ltcontrol",
    "type": "function",
    "z": "openlab_registry_v2",
    "name": "Helper LT: control RO / DO",
    "x": 760,
    "y": 420,
    "wires": [
      [
        "registry_v2_sender"
      ],
      [
        "registry_v2_tbout",
        "registry_v2_replydebug"
      ]
    ],
    "func": "const readRegistry = function readRegistry(env) {\n  let raw = env.get('DEVICE_REGISTRY');\n  if (typeof raw === 'string') raw = JSON.parse(raw);\n  if (!raw || typeof raw !== 'object' || Array.isArray(raw)) throw Error('DEVICE_REGISTRY debe ser un objeto JSON');\n  const devices = {};\n  for (const [key, entry] of Object.entries(raw)) {\n    const eui = key.toLowerCase();\n    if (!/^[0-9a-f]{16}$/.test(eui) || Object.prototype.hasOwnProperty.call(devices,eui)) throw Error('DevEUI invalido o duplicado en DEVICE_REGISTRY');\n    if (!entry || typeof entry !== 'object' || Array.isArray(entry)) throw Error('Entrada de registro invalida: '+eui);\n    const model = entry.model;\n    if (!['LT22222','AM102'].includes(model)) throw Error('Modelo no soportado en registro: '+eui);\n    const app = entry.applicationId;\n    if (!((typeof app === 'string' && /^\\d+$/.test(app)) || (Number.isSafeInteger(app) && app >= 0))) throw Error('applicationId invalido: '+eui);\n    if (!Number.isInteger(entry.fPort) || entry.fPort < 1 || entry.fPort > 223) throw Error('fPort debe ser entero 1..223: '+eui);\n    if (entry.stateMaxAgeS !== undefined && (!Number.isFinite(entry.stateMaxAgeS) || entry.stateMaxAgeS <= 0)) throw Error('stateMaxAgeS debe ser positivo: '+eui);\n    devices[eui] = {devEUI:eui,model,applicationId:String(app),fPort:entry.fPort,stateMaxAgeS:entry.stateMaxAgeS};\n  }\n  return devices;\n};\nconst reply = function reply(rpc, data) {\n  return {topic:'v1/gateway/rpc',payload:{device:rpc.device,id:rpc.id,data},qos:'1',retain:false};\n};\nconst {rpc,device} = msg;\n  try {\n    if (!rpc || !device || device.model !== 'LT22222') throw Error('Destino LT invalido');\n    const match = /^(get|set)(RO1|RO2|DO1|DO2)$/.exec(rpc.method);\n    if (!match) throw Error('Metodo LT invalido');\n    const output = match[2];\n    if (match[1] === 'get') {\n      const all = flow.get('registry_output_state') || {};\n      const cached = all[rpc.eui] && all[rpc.eui][output];\n      const maxAge = device.stateMaxAgeS === undefined ? Number(env.get('STATE_MAX_AGE_S')) : device.stateMaxAgeS;\n      if (!Number.isFinite(maxAge) || maxAge <= 0) throw Error('STATE_MAX_AGE_S debe ser positivo');\n      if (!cached || typeof cached.value !== 'boolean' || !Number.isFinite(cached.ts) || Date.now()-cached.ts > maxAge*1000) throw Error('Estado ausente o antiguo: esperar uplink reciente');\n      return [null,reply(rpc,cached.value)];\n    }\n    if (![true,false,0,1].includes(rpc.params)) throw Error('params debe ser booleano true/false o numero 0/1');\n    const active = rpc.params === true || rpc.params === 1;\n    const bytes = output.startsWith('RO') ? [3,17,17] : [2,17,17,17];\n    bytes[Number(output.slice(-1))] = active ? 1 : 0;\n    msg.command = {hex:Buffer.from(bytes).toString('hex'),details:{output,requested:active}};\n    return [msg,null];\n  } catch (err) {\n    if (!rpc) {node.error(err.message,msg);return null;}\n    return [null,reply(rpc,{accepted:false,error:err.message})];\n  }",
    "outputs": 2,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "outputLabels": [
      "Comando preparado",
      "Lectura / error RPC"
    ]
  },
  {
    "id": "registry_v2_interval",
    "type": "function",
    "z": "openlab_registry_v2",
    "name": "Helper: intervalo LT / AM102",
    "x": 760,
    "y": 500,
    "wires": [
      [
        "registry_v2_sender"
      ],
      [
        "registry_v2_tbout",
        "registry_v2_replydebug"
      ]
    ],
    "func": "const readRegistry = function readRegistry(env) {\n  let raw = env.get('DEVICE_REGISTRY');\n  if (typeof raw === 'string') raw = JSON.parse(raw);\n  if (!raw || typeof raw !== 'object' || Array.isArray(raw)) throw Error('DEVICE_REGISTRY debe ser un objeto JSON');\n  const devices = {};\n  for (const [key, entry] of Object.entries(raw)) {\n    const eui = key.toLowerCase();\n    if (!/^[0-9a-f]{16}$/.test(eui) || Object.prototype.hasOwnProperty.call(devices,eui)) throw Error('DevEUI invalido o duplicado en DEVICE_REGISTRY');\n    if (!entry || typeof entry !== 'object' || Array.isArray(entry)) throw Error('Entrada de registro invalida: '+eui);\n    const model = entry.model;\n    if (!['LT22222','AM102'].includes(model)) throw Error('Modelo no soportado en registro: '+eui);\n    const app = entry.applicationId;\n    if (!((typeof app === 'string' && /^\\d+$/.test(app)) || (Number.isSafeInteger(app) && app >= 0))) throw Error('applicationId invalido: '+eui);\n    if (!Number.isInteger(entry.fPort) || entry.fPort < 1 || entry.fPort > 223) throw Error('fPort debe ser entero 1..223: '+eui);\n    if (entry.stateMaxAgeS !== undefined && (!Number.isFinite(entry.stateMaxAgeS) || entry.stateMaxAgeS <= 0)) throw Error('stateMaxAgeS debe ser positivo: '+eui);\n    devices[eui] = {devEUI:eui,model,applicationId:String(app),fPort:entry.fPort,stateMaxAgeS:entry.stateMaxAgeS};\n  }\n  return devices;\n};\nconst reply = function reply(rpc, data) {\n  return {topic:'v1/gateway/rpc',payload:{device:rpc.device,id:rpc.id,data},qos:'1',retain:false};\n};\nconst {rpc,device} = msg;\n  try {\n    if (!rpc || !device || rpc.method !== 'setReportingInterval') throw Error('Solicitud de intervalo invalida');\n    const seconds = rpc.params;\n    const limits = {LT22222:[30,86400],AM102:[60,64800]};\n    const range = limits[device.model];\n    if (!range) throw Error('Modelo sin encoder de intervalo');\n    if (!Number.isInteger(seconds) || seconds < range[0] || seconds > range[1]) throw Error('params debe ser un numero entero de segundos entre '+range.join(' y '));\n    let bytes;\n    if (device.model === 'LT22222') {\n      bytes = Buffer.alloc(4);bytes[0]=1;bytes.writeUIntBE(seconds,1,3);\n    } else {\n      bytes = Buffer.alloc(4);bytes[0]=255;bytes[1]=3;bytes.writeUInt16LE(seconds,2);\n    }\n    msg.command = {hex:bytes.toString('hex'),details:{requestedIntervalSeconds:seconds}};\n    return [msg,null];\n  } catch (err) {\n    if (!rpc) {node.error(err.message,msg);return null;}\n    return [null,reply(rpc,{accepted:false,error:err.message})];\n  }",
    "outputs": 2,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "outputLabels": [
      "Comando preparado",
      "Error RPC"
    ]
  },
  {
    "id": "registry_v2_sender",
    "type": "function",
    "z": "openlab_registry_v2",
    "name": "Downlink generico: validar y enviar",
    "x": 1080,
    "y": 470,
    "wires": [
      [
        "registry_v2_down"
      ],
      [
        "registry_v2_tbout",
        "registry_v2_replydebug"
      ],
      [
        "registry_v2_cmddebug"
      ]
    ],
    "func": "const readRegistry = function readRegistry(env) {\n  let raw = env.get('DEVICE_REGISTRY');\n  if (typeof raw === 'string') raw = JSON.parse(raw);\n  if (!raw || typeof raw !== 'object' || Array.isArray(raw)) throw Error('DEVICE_REGISTRY debe ser un objeto JSON');\n  const devices = {};\n  for (const [key, entry] of Object.entries(raw)) {\n    const eui = key.toLowerCase();\n    if (!/^[0-9a-f]{16}$/.test(eui) || Object.prototype.hasOwnProperty.call(devices,eui)) throw Error('DevEUI invalido o duplicado en DEVICE_REGISTRY');\n    if (!entry || typeof entry !== 'object' || Array.isArray(entry)) throw Error('Entrada de registro invalida: '+eui);\n    const model = entry.model;\n    if (!['LT22222','AM102'].includes(model)) throw Error('Modelo no soportado en registro: '+eui);\n    const app = entry.applicationId;\n    if (!((typeof app === 'string' && /^\\d+$/.test(app)) || (Number.isSafeInteger(app) && app >= 0))) throw Error('applicationId invalido: '+eui);\n    if (!Number.isInteger(entry.fPort) || entry.fPort < 1 || entry.fPort > 223) throw Error('fPort debe ser entero 1..223: '+eui);\n    if (entry.stateMaxAgeS !== undefined && (!Number.isFinite(entry.stateMaxAgeS) || entry.stateMaxAgeS <= 0)) throw Error('stateMaxAgeS debe ser positivo: '+eui);\n    devices[eui] = {devEUI:eui,model,applicationId:String(app),fPort:entry.fPort,stateMaxAgeS:entry.stateMaxAgeS};\n  }\n  return devices;\n};\nconst reply = function reply(rpc, data) {\n  return {topic:'v1/gateway/rpc',payload:{device:rpc.device,id:rpc.id,data},qos:'1',retain:false};\n};\nconst rpc = msg.rpc;\n  try {\n    if (!rpc || !Number.isSafeInteger(rpc.id) || typeof rpc.device !== 'string' || rpc.eui !== rpc.device.toLowerCase()) throw Error('Falta contexto RPC valido');\n    const device = readRegistry(env)[rpc.eui];\n    if (!device) throw Error('Dispositivo no registrado para downlink');\n    const command = msg.command || {};\n    if (typeof command.hex !== 'string' || !/^(?:[0-9a-fA-F]{2})+$/.test(command.hex) || command.hex.length > 102) throw Error('Payload hex invalido o mayor que 51 bytes');\n    const hex = command.hex.toLowerCase();\n    const topic = 'application/'+device.applicationId+'/device/'+device.devEUI+'/tx';\n    const base64 = Buffer.from(hex,'hex').toString('base64');\n    const diagnostic = {payload:{device:device.devEUI,model:device.model,method:rpc.method,hex,base64,topic,fPort:device.fPort,...command.details}};\n    const signature = JSON.stringify([device.model,topic,device.fPort,rpc.method,rpc.params,hex]);\n    const now = Date.now();\n    const seen = flow.get('registry_rpc_seen') || {};\n    Object.keys(seen).forEach(k=>{if (now-seen[k].ts > 120000) delete seen[k];});\n    flow.set('registry_rpc_seen',seen);\n    const key = rpc.eui+':'+rpc.id;\n    if (seen[key]) {\n      if (seen[key].signature !== signature) throw Error('ID RPC reutilizado con otro comando o ruta');\n      return [null,reply(rpc,seen[key].result),null];\n    }\n    const enabled = env.get('ENABLE_DOWNLINK');\n    if (![true,false,'true','false'].includes(enabled)) throw Error('ENABLE_DOWNLINK debe ser booleano');\n    if (enabled === false || enabled === 'false') {\n      return [null,reply(rpc,{accepted:false,stage:'simulation',executed:false,hex,...command.details}),diagnostic];\n    }\n    const gap = Number(env.get('MIN_COMMAND_GAP_S'));\n    if (!Number.isFinite(gap) || gap < 0) throw Error('MIN_COMMAND_GAP_S debe ser cero o positivo');\n    const last = flow.get('registry_last_command') || {};\n    if (last[rpc.eui] !== undefined && now-last[rpc.eui] < gap*1000) throw Error('Esperar '+gap+' segundos entre comandos al mismo dispositivo');\n    const packet = {topic,payload:{confirmed:false,fport:device.fPort,data:base64},qos:'0',retain:false};\n    const result = {accepted:true,stage:'submitted_to_mqtt_node',executed:false,...command.details};\n    seen[key] = {ts:now,signature,result};flow.set('registry_rpc_seen',seen);\n    last[rpc.eui] = now;flow.set('registry_last_command',last);\n    return [packet,reply(rpc,result),diagnostic];\n  } catch (err) {\n    if (!rpc) {node.error(err.message,msg);return null;}\n    return [null,reply(rpc,{accepted:false,error:err.message}),null];\n  }",
    "outputs": 3,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "outputLabels": [
      "MQTT LNS",
      "Respuesta RPC",
      "Diagnostico"
    ]
  },
  {
    "id": "registry_v2_down",
    "type": "mqtt out",
    "z": "openlab_registry_v2",
    "name": "Downlink LNS local",
    "x": 1400,
    "y": 430,
    "wires": [],
    "topic": "",
    "qos": "0",
    "retain": "false",
    "broker": "registry_v2_lns"
  },
  {
    "id": "registry_v2_cmddebug",
    "type": "debug",
    "z": "openlab_registry_v2",
    "name": "Comando preparado / simulacion",
    "x": 1400,
    "y": 530,
    "wires": [],
    "active": true,
    "tosidebar": true,
    "console": false,
    "tostatus": false,
    "complete": "payload",
    "targetType": "msg"
  },
  {
    "id": "registry_v2_rpcdebug",
    "type": "debug",
    "z": "openlab_registry_v2",
    "name": "RPC recibido (activar para diagnostico)",
    "x": 420,
    "y": 600,
    "wires": [],
    "active": false,
    "tosidebar": true,
    "console": false,
    "tostatus": false,
    "complete": "true",
    "targetType": "full"
  },
  {
    "id": "registry_v2_replydebug",
    "type": "debug",
    "z": "openlab_registry_v2",
    "name": "Respuesta RPC (activar para diagnostico)",
    "x": 1080,
    "y": 620,
    "wires": [],
    "active": false,
    "tosidebar": true,
    "console": false,
    "tostatus": false,
    "complete": "true",
    "targetType": "full"
  },
  {
    "id": "registry_v2_catch",
    "type": "catch",
    "z": "openlab_registry_v2",
    "name": "Errores del flujo",
    "x": 170,
    "y": 700,
    "wires": [
      [
        "registry_v2_errors"
      ]
    ],
    "scope": null,
    "uncaught": false
  },
  {
    "id": "registry_v2_errors",
    "type": "debug",
    "z": "openlab_registry_v2",
    "name": "Error: revisar mensaje y registro",
    "x": 470,
    "y": 700,
    "wires": [],
    "active": true,
    "tosidebar": true,
    "console": false,
    "tostatus": false,
    "complete": "true",
    "targetType": "full"
  },
  {
    "id": "registry_v2_note",
    "type": "comment",
    "z": "openlab_registry_v2",
    "name": "3. Sin payload ni topic fijo en MQTT out local",
    "x": 820,
    "y": 720,
    "wires": [],
    "info": "El emisor usa application/<applicationId>/device/<DevEUI>/tx y {confirmed:false,fport:<fPort>,data:<Base64>}. No contiene codigos de fabricante. Los helpers construyen hex. Retain desactivado; QoS 0 local y QoS 1 ThingsBoard. Pausa por dispositivo y deduplicacion durante 120 s en memoria."
  },
  {
    "id": "registry_v2_lns",
    "type": "mqtt-broker",
    "name": "EDITAR CREDENCIALES - LNS local UG56",
    "broker": "127.0.0.1",
    "port": "1883",
    "clientid": "",
    "autoConnect": true,
    "usetls": false,
    "protocolVersion": "4",
    "keepalive": "60",
    "cleansession": true,
    "autoUnsubscribe": true
  },
  {
    "id": "registry_v2_tb",
    "type": "mqtt-broker",
    "name": "EDITAR TOKEN - ThingsBoard CE 3.5.0",
    "broker": "thingsboard.example.invalid",
    "port": "8883",
    "clientid": "",
    "autoConnect": true,
    "usetls": true,
    "tls": "registry_v2_tb_tls",
    "protocolVersion": "4",
    "keepalive": "60",
    "cleansession": true,
    "autoUnsubscribe": true
  },
  {
    "id": "registry_v2_tb_tls",
    "type": "tls-config",
    "name": "TLS - verificar servidor",
    "cert": "",
    "key": "",
    "ca": "",
    "certname": "",
    "keyname": "",
    "caname": "",
    "servername": "",
    "verifyservercert": true,
    "alpnprotocol": ""
  }
]
